# Tests

## Prechecks

Very rudimentary format and static type check. From `triage_library` root directory, run in a terminal

    tests/precheck.sh

Prerequisites to run these tests are `flake8` and `mypy`, which can be installed through pip.
